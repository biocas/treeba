---

---

<div class="nav-links">
    <a href="/">Latest</a>
    <a href="/tags/">Guestbook</a>
</div>

<style>
    .nav-links {
        margin-bottom: 28px;
        margin-top: 10px;
    }
    .nav-links a {
        text-decoration: none;
        color: #000A0F;
        font-family: var(--title-font);
        font-size: 14px;
        padding: 10px 14px;
        background-color: var(--background-light-green);
        border: 4px solid var(--stroke-green);
        border-radius: 11px;
    }

    .nav-links a:hover,
    .nav-links a:focus {
        background-color: var(--stroke-green);
        color: white;
        border: 4px solid var(--background-light-green);
    }
</style>
